// Customer controller placeholder

const getCustomers = async (req, res) => {
    res.json({ message: 'Get customers endpoint' });
};

const createCustomer = async (req, res) => {
    res.json({ message: 'Create customer endpoint' });
};

module.exports = { getCustomers, createCustomer };
